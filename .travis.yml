language: cpp
jobs:
  include:
  - os: osx
    script:
    - cmake -H. -B_build
    - cmake --build _build
    - cd _build
    - cpack -G DragNDrop
    - cd ..
  - os: linux
    script:
    - cmake -H. -B_build
    - cmake --build _build
    - cd _build
    - cpack -G DEB
    - cpack -G RPM
    - cd ..
addons:
  apt:
    packages:
    - cmake
    - cmake-data
    - rpm
deploy:
  provider: releases

#  file: 
#    - ./_build/solver-2.0.0.-Darwin.dmg
#    - ./_build/solver-1.0.0.-Linux.deb
#    - ./_build/solver-1.0.0.-Linux.rpm
  skip_cleanup: true
api_key:
        secure: zsiJGPNgQ2m1iHORIXptBUIAzG8D9t98hjfVS2uL1q++s87bB5hX+/so1rTiL48y
  on:
    tags: true

